# @package _global_
defaults:
  - override /training: joint
  
wandb:
  group: gravity_full
  notes: 'This is a run for dataset attractors'
  mode: 'online'

# Nested argument override of the defualt
training:
  schedule:
    epochs: [50,50]
