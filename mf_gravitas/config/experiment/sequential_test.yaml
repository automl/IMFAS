# @package _global_
defaults:
  - override /training: sequential
  
wandb:
  group: sequential_test
  notes: 'This is a test for sequential pocedure'
  mode: 'online'

# Nested argument override of the defualt
training:
  schedule:
    epochs: [10000, 10000, 10000]
