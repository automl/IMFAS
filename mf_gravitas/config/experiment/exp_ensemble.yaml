# @package _global_
defaults:
  - override /dataset_raw: yahpo
  - override /dataset: yahpo
  - override /dataset/algo_meta: yahpo_lcbench_minimal
  - override /dataset/dataset_meta: yahpo_lcbench_minimal
  - override /training: ensemble
  - override /model: rank_ensemble

wandb:
  group: rank_ensemble
  notes: 'This is a run for mlp ranking ensemble'
  mode: 'online'