# @package _global_
defaults:
  - override /dataset: yahpo
  - override /dataset/dataset_meta: yahpo_lcbench_minimal
  - override /trainer: lstm_ensemble
  - override /model: rank_lstm

wandb:
  group: rbv2_svm
  notes: 'f1_train'
  mode: 'online'


train_batch_size: 10
test_batch_size: 10

lr: 0.0005