# @package _global_
defaults:
  - override /dataset: lcbench
  - override /dataset/dataset_meta: lcbench_minimal
  - override /dataset/algo_meta: lcbench_minimal
  - override /trainer: hierarchical_transformer_ranking
  - override /model: hierarchical_transformer

dataset:
  dataset_class:
    _target_: imfas.data.Dataset_Joint_TaskswiseRanking



wandb:
  group: lcbench_vanilla_10
  notes: 'This is a run for lstm ensemble'
  mode: 'offline'

train_batch_size: 8
test_batch_size: 8

lr: 0.0005
