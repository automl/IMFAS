# @package _global_
defaults:
  - override /dataset: lcbench
  - override /dataset/dataset_meta: lcbench_minimal
  - override /dataset/algo_meta: lcbench_minimal
  - override /model: hierarchical_transformer
  - override /trainer: basetrainer


dataset:
  dataset_class:
    _target_: margret.data.Dataset_Joint_Taskwise2

wandb:
  group: lcbench_vanilla_10
  notes: 'hierarchical_transformer'
  mode: 'offline'


trainer:
  optimizer:
    lr: 0.0005

