# @package _global_
defaults:
  - override /dataset: lcbench_hierarchical
  - override /dataset/dataset_meta: lcbench_minimal
  - override /dataset/algo_meta: lcbench_minimal
  - override /dataset/lc_meta: multi_slice
  - override /trainer: basetrainer
  - override /model: hierarchical_transformer

dataset:
  dataset_class:
    _target_: masif.data.Dataset_Joint_Taskswise

  dataloader_class:
    batch_size: 8

wandb:
  group: lcbench_vanilla_10 # FIXME
  notes: 'This is a run for lstm ensemble'  # FIXME
  mode: 'offline'

trainer:
  trainerobj:
    optimizer:
      lr: 0.0005