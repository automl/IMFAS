# @package _global_
defaults:
  - override /dataset_raw: yahpo
  - override /dataset: yahpo
    # - override /dataset/algo_meta: yahpo_rbv2_glmnet_minimal
  - override /dataset/dataset_meta: yahpo_lcbench_minimal
  - override /training: lstm_ensemble
  - override /model: rank_lstm

wandb:
  group: rbv2_glmnet
  notes: 'This is a run for lstm ensemble'
  mode: 'online'


train_batch_size: 10
test_batch_size: 10

lr: 0.0005
